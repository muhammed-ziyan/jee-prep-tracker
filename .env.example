# Supabase (Database + Auth) - get from Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Service role key (server-only, for admin APIs). Never expose to client.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Admin access: comma-separated list of emails that can access /admin. Set this to your email to log in as admin.
# Alternatively, the first user in the DB gets role=admin via migration; others need role set in public.profiles.
ADMIN_EMAILS=your@email.com
